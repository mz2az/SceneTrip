# 검색 탭 네이티브 구현 계획 (iOS · Android)

- **티켓**: [MZ2AZ-148](https://mz2az.atlassian.net/browse/MZ2AZ-148) — 에픽 MZ2AZ-106 "검색·지도"
- **브랜치**: `MZ2AZ-148-목업-기반-프론트-구현` (스토리 키. 커밋은 서브태스크 키 — [AGENTS.md §7.2](../../../AGENTS.md))
- **작성일**: 2026-08-05 (2026-08-06 Android 포함으로 개정, 2026-08-07 오류 화면 절 추가)
- **모듈**: `apps/scenetrip-ios/` · `apps/scenetrip-android/`
- **순서**: **iOS 를 먼저 만든다** (§7)

> **이 문서는 작성 시점의 스냅샷이다. 정본은 `contracts/openapi/scene-api-v1.yaml` 이다.**
> §3·§4 는 그 계약을 읽고 옮겨 적은 것이므로, 계약이 바뀌면 여기가 먼저 낡는다.
> 실제로 두 번 그랬다 — MZ2AZ-167(`q` 검색폭 확장)은 §4 에 반영했고, MZ2AZ-165(500·
> `traceId`)는 하루 늦게 발견해 §3-6 으로 들어왔다. 계약을 고치는 쪽에서 이 문서를
> 함께 보도록 [AGENTS.md](../../../AGENTS.md) 의 Definition of Done 에 항목을 더해 뒀다.

---

## 1. 무엇을 만드는가

앱의 **작품검색 탭**을 iOS 네이티브(Swift)와 Android 네이티브(Kotlin)로 만든다. `apps/` 에
들어가는 첫 앱 모듈이므로, 화면 구현만이 아니라 **저장소에 모바일이라는 갈래를 여는 일**을
함께 한다 — Bazel Apple·Android 규칙 활성화, macOS CI 잡, OpenAPI 클라이언트 생성기 연결이
전부 이 계획에 딸려 있다.

화면은 이미 한 번 만들어 봤다. Flutter 로 만든 프로토타입이 `~/workspace/mobile` 에 있고
iOS 시뮬레이터에서 동작한다. **그 코드는 이 저장소로 옮기지 않는다.** [ADR 0002](../../architecture/adr/0002-product-stack-spring-python-native-mobile.md)
가 크로스 플랫폼 프레임워크를 쓰지 않기로 확정했기 때문이다. 프로토타입이 남기는 것은
코드가 아니라 **확정된 화면 구조와 검색 규칙**이며, 그 내용이 §3 이다.

프로토타입을 버리지 않고 남겨 두는 이유는 두 가지다. 화면 동작을 눈으로 비교할 대조군이
되고, §3 의 규칙이 애매할 때 실제로 돌려 볼 수 있다.

### 두 앱을 같이 계획하는 이유

ADR 0002 가 감수하기로 한 비용이 **"모바일 기능을 두 번 만든다"** 이고, 같은 문서가 검증
항목으로 **"두 모바일 앱이 실제로 어긋나지 않는가"** 를 걸어 뒀다. 어긋남은 화면을 만들 때가
아니라 **규칙이 한쪽에만 적혀 있을 때** 생긴다. 그래서 §3 을 두 앱의 공통 기준으로 두고,
플랫폼마다 다른 것은 §5 에 몰아 둔다.

## 2. 이 계획의 범위 밖

| 항목 | 어디로 |
| --- | --- |
| 경로여정 · 커뮤니티 · 마이페이지 탭 | 자리만 만들고 비운다. 별도 에픽 |
| 루트(코스) 만들기 | 스프린트 결정 — 장바구니는 담기까지만 |
| 로그인 · 개인 맞춤 추천 | 사용자 테이블이 없어 보류 |
| 스토어 배포용 빌드 | MZ2AZ-148 완료 조건이 "로컬 실행"까지다 |
| 백엔드 API 구현 | [scene-api-search-map.md](./scene-api-search-map.md) (MZ2AZ-149) |

## 3. 프로토타입이 확정한 것

Flutter 로 만들면서 여러 차례 고쳐 잡은 결과다. **iOS 와 Android 구현 모두 이것을 기준으로
삼는다.** 두 앱이 어긋나지 않게 하는 장치가 이 절이다 — 여기 없는 동작은 어느 쪽에서도
임의로 만들지 않는다.

### 3-1. 화면 구조

- 지도는 **항상 보인다.** 그 위에 바텀시트가 얹혀 3단으로 드릴다운한다 — 목록 → 작품 상세 → 촬영지 상세.
- 시트는 3단 스냅(14% / 42% / 최대)이고, 최대 높이는 검색바 바로 아래까지 기기별로 계산한다.
- 목록은 **작품 / 장소** 두 탭으로 나뉜다. 검색어 하나로 두 탭이 동시에 채워지며, 사용자가 탭을 먼저 고르지 않는다.
- 상단에 검색창과 장바구니가 한 덩어리로 고정된다.

시트 컴포넌트는 플랫폼마다 이름이 다르다(iOS `UISheetPresentationController` 계열,
Android `BottomSheetBehavior`). **스냅 비율과 최대 높이 계산은 위 숫자를 그대로 맞춘다** —
컴포넌트 기본값에 맡기면 두 앱이 미묘하게 달라진다.

### 3-2. 검색이 걸리는 범위

| 입력 | 작품 탭 | 장소 탭 |
| --- | --- | --- |
| 작품 제목·별칭 (`도깨비`, `Goblin`, `케데헌`) | 그 작품 | 그 작품의 촬영지 |
| **배우·감독 이름** (`김수현`) | 그 사람의 작품 | **그 작품들의 촬영지 전부** |
| 장소명 (`북촌한옥마을`) | — | 그 장소 |
| 지역 (`마포구`) | — | 그 지역의 촬영지 |

배우로 검색했을 때 **장소 탭까지 채워지는 것** 이 핵심이다. 프로토타입에서 이것이 빠져 있을
때는 "김수현을 쳤는데 촬영지가 안 나온다"가 되어 탭 하나가 죽은 화면이 됐다.

#### 마지막 줄(지역)은 아직 서버가 하지 않는다 (2026-08-07 실측)

앱을 서버에 붙이고 위 네 줄을 실제로 두드려 본 결과다.

| 검색어 | 작품 탭 | 장소 탭 | 표대로인가 |
| --- | --- | --- | --- |
| `도깨비` | 1 | 57 | 맞음 |
| `공유` (배우) | 1 | **57** | 맞음 — 배우로 장소 탭이 채워진다 |
| `북촌한옥마을` | 2 | 1 | 맞음 |
| `종로구` (지역) | 0 | **0** | **아니다** |

데이터가 없어서가 아니다. `place_i18n.address` 에 `종로구` 가 들어간 행이 23 건 있는데도
0 이 나온다. 원인은 계약이다 — `GET /places` 의 `q` 설명이 이렇게 적혀 있고 **주소가
빠져 있다.**

> 장소명·별칭·장소 설명·등장 작품의 제목·별칭·설명·출연진 이름을 함께 검색

즉 서버는 계약대로 동작하고 있고, **앞서간 것은 이 표다.** §3-2 는 프로토타입에서
뽑아낸 것인데 프로토타입은 주소를 클라이언트에서 직접 훑었다(`_rowMatches` 가
`r.address` 를 본다). 서버로 옮기면서 그 경로가 빠진 것을 §4 대조 때 잡지 못했다.

**프론트에서 우회하지 않는다.** 지역으로 거르려면 전체 장소를 받아 와 앱이 주소를
훑어야 하는데, 그러면 §4 가 MZ2AZ-167 을 두고 "`personId` 를 더하는 대신 `q` 를 넓히는
쪽이 낫다" 고 한 판단을 정면으로 뒤집는 셈이고, 같은 코드가 iOS·Android 에 두 벌로
복제된다. 권호와 상의할 항목으로 §6 에 올린다.

### 3-3. 자동완성

- 검색창에 포커스가 있는 동안만 뜬다. 시트를 끌거나 지도를 만지면 닫힌다 — 안 그러면 결과 목록을 가린다.
- 검색창이 비어 있으면 **최근 검색어** 와 **추천 검색어** 를 깔아 준다.
- 글자를 치면 **작품 → 인물 → 장소 → 연관 검색어** 순으로 보여 준다.
- 랭킹은 **앞글자 일치 > 단어 앞 일치 > 중간 포함** 이고, 동점이면 인기도순이다.
- 작품이 배우로 걸린 경우 `출연 김수현` 배지를 달아 제목으로 걸린 것과 구분한다.

### 3-4. 연관 검색어는 별칭 컬럼에 몰아넣지 않는다

별칭 컬럼은 "그 작품의 다른 표기"라는 뜻이 분명한 자리다. 거기에 배우·지역·장르까지 넣으면
컬럼의 의미가 뭉개지고, 배우 한 명이 작품 10편에 나오면 이름이 10번 중복 저장된다.

대신 이미 있는 컬럼에서 검색어를 파생시킨다. 프로토타입은 적재 시점에 작품 제목·별칭, 배우,
감독, 장소명, 지역(주소 앞 두 토막), 장르·채널을 훑어 검색어 사전을 만들었다.

**서버 스키마가 이미 같은 방식이다.** `search_term` 이 테이블이 아니라 다섯 곳을 `UNION ALL`
로 합친 MATERIALIZED VIEW 다([scene-api-search-map.md §5](./scene-api-search-map.md)). 프론트가
따로 만들 필요가 없고, 목데이터를 걷어내면 그대로 서버 것을 쓴다.

### 3-5. 지도와 목록을 어긋나지 않게 한다

- 검색이 **확정되는 시점**(추천어 선택·엔터)에 핀을 결과로 갈고 그 범위로 카메라를 맞춘다.
- 글자마다 갱신하지 않는다. 프로토타입에서 핀 아이콘을 위젯에서 이미지로 굽는 구조라 타이핑마다 수십 개를 다시 그리면 버벅였다. 네이티브에서도 같은 판단을 유지한다.
- **카테고리 칩은 목록과 지도를 둘 다 좁힌다. 카메라는 건드리지 않는다.**

프로토타입은 칩이 목록만 좁히고 지도 핀은 검색 결과 전체를 유지하도록 만들었으나,
실제 숫자를 보고 뒤집었다. `도깨비` 촬영지는 58 곳이고 그중 음식점·카페는 12 곳이다.

| | 목록만 좁힘 (프로토타입) | 둘 다 좁힘 (확정) |
| --- | --- | --- |
| 목록 | 12 곳 | 12 곳 |
| 지도 핀 | **58 개** | 12 개 |

목록에 없는 핀 46 개가 지도에 남으면 그 핀을 눌렀을 때 목록에 없는 장소가 열린다.
"목록과 지도가 다른 얘기를 하는" 상태가 핀이 줄어드는 것보다 더 혼란스럽다고 봤다.
카카오맵·네이버지도도 같은 방식이라 사용자 관습에도 맞는다.

**카메라는 옮기지 않는다.** 핀만 갈면 지도가 튀지 않으면서 핀은 정직해진다. 칩을
껐다 켤 때 원래 보던 위치를 잃지 않는 것이 이 선택의 목적이다.

> **칩 자체는 아직 만들지 못한다.** 서버가 `place.type` 을 한국어 라벨 37 종 그대로
> 내려주고 있어 칩으로 묶을 값이 없다 — §4 참고. 위 규칙은 칩이 생기는 시점에
> 적용한다. 규칙을 지금 못 박아 두는 이유는, 나중에 iOS 와 Android 가 각자 정하면
> 두 앱이 갈리기 때문이다.

### 3-6. 오류가 났을 때

프로토타입이 정해 주지 못한 항목이다. 목데이터로 돌았으므로 서버 오류라는 상태가
없었다. 계약([MZ2AZ-165](https://mz2az.atlassian.net/browse/MZ2AZ-165))이 오류 응답을
명시하면서 화면이 답해야 할 질문이 됐다.

**`message` 를 사용자에게 그대로 보여 주지 않는다.** 계약이 못 박아 뒀다 —
`scene-api-v1.yaml` 의 `ApiError.message` 설명이 "사람이 읽는 설명. **사용자에게 그대로
보여 줄 문구가 아니다**". 개발자·로그용이다. 화면 문구는 앱이 상태 코드별로 갖는다.

| 상태 | 언제 | 화면 |
| --- | --- | --- |
| `400` · `409` | 앱이 잘못 보냈다 | 사용자 잘못이 아니므로 원인을 설명하지 않는다. 일반 오류 문구 |
| `404` | 없는 작품·장소 | 빈 상태로 보여 준다. 오류 화면이 아니다 |
| `500` | 서버가 처리 못 함 | **재시도 버튼** + `traceId` 노출 |

**`traceId` 는 `500` 에만 실린다.** 다른 코드에서는 키 자체가 응답에서 빠지므로
"없으면 숨긴다"가 아니라 **`500` 일 때만 그리는** 것으로 짠다. 계약의 지시가 명시적이다.

> 사용자가 그 문자열 하나를 알려 주면 서버 쪽에서 그 요청의 트레이스와 로그를 그대로
> 찾을 수 있으므로, **화면에 보여 주거나 오류 보고에 함께 담는다.**

**재시도는 `500` 에만 건다.** 계약이 "재시도해도 된다 — 클라이언트가 고칠 것은 없다"고
적은 유일한 코드다. `400`·`409` 에 재시도를 걸면 같은 요청을 그대로 다시 보내 같은
오류를 받는다.

#### 팀 확인이 필요한 것

아래 두 가지는 두 앱에 같이 적용돼야 하므로 **여기서 정하고 넘어간다** — iOS 를 만들며
즉흥으로 고르면 Android 가 따라올 근거가 없다. 초안을 적어 두되 확정은 팀 논의로 한다.

| # | 항목 | 초안 |
| --- | --- | --- |
| 1 | `traceId` 를 사용자에게 어떻게 보여 줄까 | 오류 화면 하단에 작게 + **길게 눌러 복사**. 외국인 대상 앱이라 받아 적게 하는 것은 무리다 |
| 2 | 오류 문구의 언어 | 앱의 표시 언어를 따른다(MVP1 = 한국어·영어). 서버 `message` 는 번역 대상이 아니다 — 애초에 노출하지 않으므로 |

## 4. API 계약과 대조 — 다 맞는다

[`contracts/openapi/scene-api-v1.yaml`](../../../contracts/openapi/scene-api-v1.yaml) 을 §3 과
맞춰 봤다. 프로토타입은 이 명세를 보지 않고 만들었는데도 결론이 같다.

| §3 의 요구 | 명세 | 상태 |
| --- | --- | --- |
| 자동완성에 작품·인물·장소가 섞여 나온다 | `GET /search/suggestions` 가 `type` 으로 place·content·person 을 구분해 돌려준다 | 맞음 |
| 누른 항목이 어디로 가는지 안다 | 각 항목이 `type` 과 `id` 를 들고 있다 | 맞음 |
| 배우 이름으로 작품이 걸린다 | `GET /contents?q=` 가 인물 이름에 걸린다 | 맞음 |
| 자동완성에서 인물을 누르면 그 사람 작품 | `GET /contents?personId=` | 맞음 |
| 작품명으로 장소 탭이 채워진다 | `GET /places?q=` 가 등장 작품 제목·별칭에 걸린다 | 맞음 |
| 랭킹 = 앞글자 우선, 인기도순 | 명세에 같은 규칙이 적혀 있다 | 맞음 |
| **배우 이름으로 장소 탭이 채워진다** | `GET /places?q=` 가 **출연진 이름에도 걸린다** | 맞음 |
| 오류가 났을 때 무엇을 보여줄지 안다 (§3-6) | `components/responses/InternalError` 와 `ApiError.traceId` 가 9 개 엔드포인트 전부에 선언돼 있다 | 맞음 — **2026-08-07 추가** |

마지막 줄은 이 문서가 뒤늦게 따라간 항목이다. 초안(8/5)과 개정(8/6) 시점에는 명세에
`500` 선언이 없어 대조할 것이 없었다. 8/6 밤 [MZ2AZ-165](https://mz2az.atlassian.net/browse/MZ2AZ-165)
가 그것을 메우면서 화면이 답해야 할 질문이 생겼고, 그 답이 §3-6 이다.

### 마지막 줄은 서버가 이미 메웠다 (MZ2AZ-167)

이 계획의 초안(8/5)을 쓸 때는 마지막 줄이 비어 있었다. `GET /places` 의 `q` 가 장소명·별칭·
장소 설명·**등장 작품의 제목** 까지만 봤기 때문에, `김수현` 을 치면 작품 탭은 채워지는데
장소 탭이 비었다. §3-2 가 죽는 상태였다.

8/6 에 권호가 고쳤다(커밋 `499c5d1`, MZ2AZ-167). **`personId` 파라미터를 더하는 대신 `q` 의
검색 폭 자체를 넓히는** 쪽을 택했다. 장소 쪽에 두 경로가 추가됐다.

```
작품 설명  → place_content → 장소
인물 이름  → content_cast → place_content → 장소
```

명세의 `q` 설명도 그에 맞게 바뀌었다.

> 장소명·별칭·장소 설명·등장 작품의 제목·별칭·**설명·출연진 이름**을 함께 검색

**이 선택이 초안이 제안했던 `personId` 방식보다 낫다.** `personId` 를 더했다면 프론트가
"이름을 쳤다"와 "자동완성에서
인물을 골랐다"를 구분해 호출을 갈라야 했다. `q` 를 넓히면 **작품 탭과 장소 탭이 같은
파라미터, 같은 문자열로 채워진다** — 프론트는 `GET /contents?q=` 와 `GET /places?q=` 에 같은
값을 넣기만 하면 되고, 그 코드가 iOS 와 Android 에서 같은 모양이 된다.

[scene-api-search-map.md §5](./scene-api-search-map.md) 가 근거를 이렇게 적어 뒀다 —
"한쪽 탭에 걸리는 것은 다른 탭에도 걸려야 한다. 결과의 종류만 다를 뿐 질문은 하나다."
§3-2 표가 말하려던 것과 같은 얘기다.

검증은 서버 쪽에서 이미 끝났다. `공유`·`김고은`·`도깨비` 셋 다 작품 탭 1건 · 장소 탭 3건으로
같이 채워진다.

**프론트가 할 일은 없다.** 우회 로직도, 추가 파라미터도 필요 없다.

### 프론트가 알고 있어야 할 명세의 한계

[scene-api-search-map.md §4·§5](./scene-api-search-map.md) 가 적어 둔 것이다. 화면에서
빈칸이 보일 때 버그로 오해하지 않으려면 미리 알아야 한다.

- **장소는 영어로 검색되지 않는다.** 작품에는 영어 별칭이 있지만 장소명에는 없다. 다만 위 확장 덕에 **영어 작품명·배우명으로는 장소가 걸린다** — `Goblin` 이 도깨비 촬영지를 부른다.
- **설명 검색은 당분간 아무것도 잡지 못한다.** 작품 설명은 4편 중 1편만 있고, 장소 설명은 수집 컬럼 자체가 없다. MZ2AZ-167 이 "작품 설명 → 장소" 경로를 열어 뒀지만 채울 데이터가 아직 없다.
- **장면 설명(`relation_description`)은 검색되지 않는다.** 장소와 작품 사이에 붙은 값이라 어느 탭의 대상으로도 삼지 않기로 했다.

### 카테고리 칩으로 쓸 값이 아직 없다

`PlaceSummary.type` 이 계약서에 이렇게 적혀 있다.

> 수집된 장소 유형을 가공 없이 그대로 돌려준다. 현재 **37종의 자유 문자열**이라 필터
> 값으로 쓰기에 적합하지 않다 — 목업의 카테고리 칩으로 묶는 매핑은 데이터 정제 후에
> 정한다.

`place.type` 은 DB 에서도 제약 없는 `TEXT` 다. Enum 도 CHECK 도 없고, 한국어 라벨이
그대로 들어간다(`자연`·`음식점`·`다리`·`한옥마을`·`명소`·`상점`·`공원`·`전망대`·`거리`…).
같은 장소가 행마다 다른 타입으로 수집된 경우도 있다 — 일월수목원이 `자연` 과 `공원`
둘로 들어와 `ORDER BY` 로 어느 쪽이 이기는지만 고정해 뒀다.

프로토타입은 37종을 4묶음으로 접는 매핑표(`kCategoryChips`)를 **클라이언트 안에** 들고
있다. 그대로 옮기면 **같은 표가 iOS 와 Android 에 두 벌로 복제되고**, 고치려면 앱을
다시 배포해야 한다. `apps/README.md` 가 금지하는 형태다 — "두 앱이 같은 규칙을 지켜야
한다면 공유하는 것은 코드가 아니라 `contracts/` 의 정의다."

**그래서 1차 이식에서는 칩을 만들지 않는다.** 목록과 지도만 만든다. 서버가 묶어 준
값을 내려주면 그때 §3-5 의 규칙대로 붙인다. 권호에게 요청할 항목으로 §6 에 올린다.
- **인기도는 v1 에서 적재 시 고정한 임의값이다.** 정렬에만 쓰이고 응답에는 안 들어온다.
- 목업에 있으나 v1 응답에 없는 필드(신뢰도 배지·회차 타임코드·명대사·운영시간 등)는 그 문서 §6 에 표로 있다.

## 5. 모바일이라는 갈래를 여는 데 필요한 것

화면 코드보다 이쪽이 먼저 막힌다. **iOS 와 Android 는 막히는 지점이 서로 다르다.**

| | iOS (Swift) | Android (Kotlin) |
| --- | --- | --- |
| Bazel 규칙 | `apple_support` · `rules_swift` · `rules_apple` | `rules_kotlin` · `rules_android` |
| 밖에서 깔아야 하는 것 | **정식 Xcode** (10~15GB) | Android SDK |
| 빌드되는 OS | **macOS 만** | 리눅스에서도 된다 |
| CI | **macOS 잡을 새로 만들어야 함** | 기존 ubuntu 잡이 그대로 처리 |
| BUILD 파일 | Gazelle 이 만들어 준다 (`swift_gazelle_plugin`) | **손으로 쓴다 — 쓸 만한 Gazelle 확장이 없다** |
| 스캐폴드 | `just new-app-ios <name>` | `just new-app-android <name>` |

이 비대칭이 §7 작업 순서를 정하는 근거다. 준비 비용만 보면 Android 가 가볍지만
**iOS 를 먼저 만들기로 했다** — 이유는 §7 에 적는다.

### 5-1. Bazel 규칙 활성화

`MODULE.bazel` 에 두 블록이 주석으로 대기하고 있다.

- **Swift/Apple**: `apple_support` 2.8.0, `rules_swift` 3.6.1, `rules_apple` 4.5.3,
  `rules_swift_package_manager` 0.34.0
- **Kotlin/Android**: `rules_kotlin` 2.4.0, `rules_android` 0.7.3

**Apple 블록은 팀 전체에 영향이 있다.** ADR 0002 의 실측 기록에 따르면 `apple_support` 를
선언하기만 해도 정식 Xcode 없이 Command Line Tools 만 있는 macOS 에서는 iOS 타깃이 하나도
없는데도 `bazel build //...` 전체가 크래시한다(`DottedVersion` 파싱 실패). 즉 이 규칙을 푸는
순간 **백엔드만 만지는 권호와 AI 만 만지는 태환도 Xcode 를 설치해야** `just check` 가 돈다.

푸는 시점을 두 사람에게 미리 알린다. 세 명 모두 맥북을 쓰므로 설치 자체는 가능하지만
10~15GB 다운로드가 걸린다. **Android 블록에는 이런 부작용이 없다** — 먼저 풀어도 안전하다.

### 5-2. Android BUILD 파일은 손으로 쓴다

ADR 0002 와 `MODULE.bazel` 이 같은 내용을 적어 뒀다. Aspect 의 Kotlin Gazelle 플러그인은
스스로 EXPERIMENTAL 이라 밝히고 `kt_jvm_library`·`kt_jvm_binary` 만 만든다. 우리가 정작
필요한 `android_library`·`android_binary`·`kt_android_library` 를 만들어 주지 못하고,
Gazelle 에 Android 확장은 공식·비공식 모두 사실상 없다.

그래서 `just gen` 이 Android 모듈의 BUILD 파일을 갱신해 주지 않는다. **소스를 추가할 때마다
BUILD 파일을 직접 고쳐야 하고, 잊으면 "파일은 있는데 빌드에 안 들어간" 상태가 된다.**
Android 모듈 README 에 이 사실을 적어 둔다.

### 5-3. iOS 타깃을 리눅스 러너로부터 지킨다

초안은 `target_compatible_with = ["@platforms//os:macos"]` 한 줄이면 리눅스 러너가 iOS
타깃을 조용히 건너뛴다고 봤다. **구현하면서 실측하니 틀렸다** (macOS 로컬 + 우분투 컨테이너,
2026-08-06). 두 가지가 어긋난다.

- `ios_application` 은 빌드 시 타깃 플랫폼을 `ios_sim_arm64` 등으로 **전환**하고,
  `target_compatible_with` 는 그 전환 **뒤에** 평가된다. macos 만 허용하면 전환된 iOS
  플랫폼이 제약을 만족하지 못해 `:bin` 이 **어디서도** 지어지지 않는다 — 와일드카드 빌드는
  비호환 타깃을 조용히 건너뛰므로 `just check` 는 초록인 채로 앱 번들만 빠진다.
- 전환된 플랫폼은 호스트가 리눅스든 macOS 든 같은 값이라 **호스트 OS 를 구분하지 못한다.**
  리눅스에서는 조용한 스킵이 아니라 **툴체인 해석 실패(하드 에러)** 가 난다 — 명시 요청이든
  와일드카드든 마찬가지다.

실제로 쓰는 장치는 두 겹이다 (`apps/scenetrip-ios/BUILD.bazel` 머리말에도 같은 내용).

```python
# BUILD.bazel — 모든 iOS 타깃에
tags = ["ios"]
target_compatible_with = select({
    "@platforms//os:ios": [],
    "@platforms//os:macos": [],
    "//conditions:default": ["@platforms//:incompatible"],
})
```

```
# .bazelrc — enable_platform_specific_config 가 호스트 OS 이름의 config 를 자동 적용
build:linux --build_tag_filters=-ios
test:linux  --test_tag_filters=-ios
```

리눅스를 실제로 지키는 것은 **태그 필터**다. select 는 "Apple 플랫폼에서만 지어진다"는
의미 선언으로 남긴다 — 검사 회피가 아니라 실행 환경 제약을 코드에 적는 것이라는 취지는
그대로다. 이렇게 하면 같은 `just ci-full` 이 리눅스 잡과 macOS 잡 양쪽에서 각자 할 일을
하게 된다.

두 가지 유의점.

- 현행 우분투 레인이 아직 안 깨진 것은 `--build_tests_only` 덕이다 — `bazel test //...` 가
  테스트 아닌 타깃(`:bin`)을 아예 분석하지 않는다. 태그 필터는 `just build`(전체 빌드)와
  앞으로 생길 iOS 테스트 타깃을 지키는 장치다.
- 테스트 레인 레시피가 명령줄에서 `--test_tag_filters` 를 넘기면 `.bazelrc` 값을
  **덮어쓴다**(태그 필터는 누적이 아니다). 첫 iOS 테스트 타깃이 생기면 그 레시피들에
  `-ios` 를 함께 넣는다 — §5-4 의 CI macOS 잡과 같은 시점에 정리한다.

**Android 타깃에는 태그도 제약도 붙이지 않는다.** 리눅스에서도 지어지므로 붙이면 오히려
기존 ubuntu 잡의 검사 범위에서 빠진다.

`target_compatible_with` 용례 설명은 `docs/engineering/onboarding.md` 격리 절에 남겼다.

### 5-4. CI 에 macOS 잡을 추가한다

ADR 0002 의 후속 작업 "CI 에 macOS 러너 잡 추가 — 첫 iOS 모듈이 생길 때" 가 지금이다.

`.github/workflows/ci.yml` 의 `verify` 잡은 `ubuntu-latest` 에서 돈다. 리눅스에는 Xcode 가
없어 iOS 앱을 지을 수 없으므로 `macos-latest` 잡을 하나 더 둔다. 워크플로는 얇은 껍데기를
유지하고 파이프라인 로직은 `tools/just/ci.just` 에 남긴다.

저장소가 공개이므로 GitHub 표준 러너는 무료다. 비공개였다면 macOS 는 분 단위 사용량이
10배로 차감돼 미루는 쪽이 합리적이었으나, 공개 전환으로 그 제약이 없어졌다.

#### 초안이 놓친 것 — Swift 게이트를 어느 잡이 지는가 (2026-08-07)

초안은 이 절을 "잡 하나 추가"로만 봤다. 구현하면서 그것으로 부족하다는 것이 드러났다.

`lint.sh` · `format.sh` 는 언어 적용 여부를 **파일 존재만으로** 판단한다
(`has_files '*.swift'`). 그래서 `SceneTripApp.swift` 가 들어온 순간 **우분투 `verify`
잡에서도** Swift 절이 켜졌는데, 그 러너에는 `swiftlint` · `swiftformat` 이 없고 두
스크립트는 도구가 없으면 `die` 한다 — 즉 **다음 PR 부터 우분투 잡이 Swift 게이트에서
죽는 상태**였다. §5-6 이 예고한 "`.swift` 가 하나라도 생기는 순간 자동으로 켜진다" 가
로컬만이 아니라 CI 에도 걸린 것이다.

아직 터지지 않은 이유는 트리거뿐이다. 워크플로가 `pull_request` 와 `push: main` 에서만
돌고, iOS 모듈 커밋 두 개는 그 뒤로 열린 PR 이 없어 CI 를 한 번도 타지 않았다.

**Swift 검사를 Apple 플랫폼으로 한정했다.** `.bazelrc` 가 리눅스에서 `ios` 태그를
걸러내는 것과 같은 경계다 — 리눅스는 이 모듈을 짓지도 않으므로 린트만 도는 것이 앞뒤가
맞지 않고, 리눅스용 swiftlint 는 별도 배포라 설치가 느리고 잘 깨진다.

**조용히 건너뛰지는 않는다.** `lint.sh` 머리말의 기준이 "검사해서 통과한 것과 아예
검사하지 않은 것은 다르다" 이므로, 건너뛸 때 경고를 남기고 **누가 대신 검사하는지**를
함께 적었다(`verify-macos`). 그래서 이 절의 잡은 편의가 아니라 **Swift 를 검사하는
유일한 곳**이다 — 지우면 Swift 는 어디서도 검사되지 않는다.

macOS 러너 이미지에 두 도구가 들어 있다는 보장이 없어 `brew install` 단계를 명시했다.

**Android 는 이 작업을 기다리지 않는다.** 기존 ubuntu 잡이 그대로 짓는다. 다만 러너에
Android SDK 가 필요하므로 `ubuntu-latest` 이미지에 포함된 것을 쓸지 설치 단계를 넣을지는
모듈을 만들면서 확인한다.

### 5-5. 클라이언트 생성기를 붙인다

[scene-api-search-map.md §9](./scene-api-search-map.md) 가 예고해 둔 작업이다. 서버 인터페이스
(`spring`)는 이미 `contracts/openapi/BUILD.bazel` 에 붙어 있고, `MODULE.bazel` 이
**"생성기 이름만 바꾸면 언어가 바뀐다 — `spring` · `swift5`(iOS) · `kotlin`(Android)"** 라고
적어 뒀다. 앱 모듈이 생기는 지금이 그 시점이다.

**앱은 API 클라이언트를 손으로 쓰지 않는다**([CLAUDE.md §5](../../../CLAUDE.md)). 명세에서
생성한 것을 쓴다. 두 앱이 같은 명세에서 생성한 클라이언트를 쓰는 것이 §1 의 "어긋나지 않게
한다"를 지키는 가장 값싼 방법이기도 하다.

같은 문서가 그 시점에 **도구 선택을 ADR 로 승격** 하라고 적어 뒀다 —
`openapi_tools_generator_bazel` 의 유지보수자 부재 위험을 기록으로 남겨야 하기 때문이다.

**번호는 여기에 적지 않는다.** 초안은 "다음 번호는 0004" 라고 못 박았는데, 그 사이
0004(OpenTelemetry) 와 0005(통합 테스트 의존성) 가 들어와 빗나갔다. ADR 번호는
`just adr-new` 가 만드는 시점에 선착순으로 매겨지므로, 며칠 뒤에 실행할 문서가 미리
예약할 수 있는 값이 아니다. 쓸 때 `just adr-list` 로 다음 번호를 확인한다.

#### 완료 (2026-08-07) — [ADR 0006](../../architecture/adr/0006-openapi-client-generation.md)

`swift5` 생성 타깃과 그것을 앱이 쓰는 `scene_api_swift_lib` 를 붙였다. 두 가지가
예상 밖이었고 ADR 0006 의 "감수하는 비용" 에 적었다 — 생성물을 그대로
`swift_library` 에 넣을 수 없어 `tools/bazel/defs/merge_tree_sources.bzl` 가 필요했고,
명세의 `Scene` 모델이 SwiftUI 의 `Scene` 과 부딪혀 한정이 필요했다.

**목데이터는 걷어냈다.** 화면이 서버를 직접 부르므로 `SceneData` 안에 검색 로직이
없다 — 서버가 하는 일을 앱이 다시 구현하면 두 앱이 갈린다.

### 5-6. 포매터·린터

배선은 이미 다 돼 있고 엄격하다. `tools/scripts/lint.sh`·`format.sh` 에 Swift 절이
쓰여 있어 **`.swift` 파일이 하나라도 생기는 순간 자동으로 켜진다.** 도구가 없으면
게이트가 빨간불이 된다 — `lint.sh` 주석의 표현대로 "검사해서 통과한 것과 아예
검사하지 않은 것은 다르다."

| 명령 | `swiftformat` 이 없을 때 |
| --- | --- |
| `just fmt` | 경고만 하고 넘어간다 (고쳐 주는 편의 명령이라) |
| `just fmt-check` · `just check` · CI | **실패한다** |

그래서 첫 Swift 파일을 만들기 전에 `brew install swiftformat swiftlint` 가 필요하다.
빠뜨리면 커밋 직전에 게이트가 막는다 — MZ2AZ-160 의 완료 조건에 넣어 둔다.
`.editorconfig` 에는 Swift 4 칸 들여쓰기가 이미 있다.

**규칙 설정 파일(`.swiftlint.yml`·`.swiftformat`)은 지금 만들지 않는다.** §6 의 남은 것 #4
참고 — 빈 프로젝트에 먼저 쓰면 무엇을 켜고 끌지 모르는 채로 남의 기본값을 베끼게 된다.

## 6. 후속 결정 사항

### 정한 것

| 항목 | 결정 |
| --- | --- |
| 앱 모듈 이름 | `apps/scenetrip-ios/` · `apps/scenetrip-android/`. `MODULE.bazel` 의 `apps/<name>-ios/` 규칙과 `services/scene-api` 의 명명을 따랐다 |
| iOS · Android 순서 | **iOS 먼저** (§7) |
| MVP1 지원 언어 | **한국어 + 영어**. 일본어는 이번 범위 밖이나 구조는 언어 추가를 전제로 짠다 |
| 카테고리 칩 동작 | **목록과 지도를 둘 다 좁힌다. 카메라는 그대로** (§3-5). 단 칩 자체는 서버가 값을 묶어 줄 때까지 보류 |

MVP1 에 영어가 들어가면서 §4 의 "장소는 영어로 검색되지 않는다" 가 감수 사항에서
**해결해야 할 항목**으로 바뀐다. 아래 #2 다.

### 남은 것

| # | 항목 | 성격 |
| --- | --- | --- |
| 1 | `place.type` 을 칩 단위로 묶어 API 가 내려줄 수 있는지 | 권호와 상의. 프론트가 매핑표를 들면 iOS·Android 에 두 벌로 복제된다 (§4) |
| 2-1 | **지역(구 이름)으로 장소가 검색되지 않는다** | 계약의 `q` 설명에 주소가 빠져 있다. 프로토타입은 주소를 훑었고 §3-2 표가 그것을 옮겨 적었으나 서버에는 그 경로가 없다(실측). `q` 를 넓히는 쪽이 MZ2AZ-167 과 같은 방식이다. 권호와 상의 |
| 2 | 장소명 영어 표기 | `place_i18n` 에 `ko` 만 있고 `place_alias` 는 비어 있다. MVP1 에 영어가 포함되므로 `Bukchon Hanok Village` 로 못 찾는 것이 문제가 된다. 권호와 상의 |
| 3 | 클라이언트 생성기 ADR | `swift5` 타깃을 붙이는 시점에 승격. 번호는 그때 `just adr-list` 로 확인한다 (§5-5) |
| 4 | Swift 린터·포매터 설정 파일 | `.swiftlint.yml`·`.swiftformat` 이 없다. 화면을 몇 개 옮긴 뒤 **실제로 나온 경고를 근거로** 쓴다. 빈 프로젝트에 먼저 쓰면 남의 기본값을 베끼게 된다 (§5-6) |
| 5 | 최근 검색어를 서버로 옮길지 | 프로토타입은 기기에만 저장했다. 로그인이 붙을 때 재논의 |
| 6 | Android 착수 시점 | iOS 화면이 어디까지 나온 뒤 시작할지. §3 이 공통 기준이므로 언제 시작해도 어긋나지는 않는다 |

#### 저장소 전체 항목 — 승길 티켓 밖, 권호와 상의

| 항목 | 무엇이 문제인가 |
| --- | --- |
| `just ci-security` 가 껍데기 | `security-scan.sh` 가 미구현인데 CI 잡은 초록으로 통과한다. 저장소가 공개로 바뀌었으므로 실제 시크릿 탐지가 필요하다 |
| 통합 테스트 0 개인데 `ci 통과` | `just ci` 가 "일치하는 테스트 대상이 없습니다" 경고를 내고도 통과한다. 나중에 태그를 잘못 달면 한 건도 안 돌았는데 초록으로 보인다 |
| 정적 분석 층이 없다 | `.bazelrc` 가 "`tools/bazel/defs/` 에 린트 애스펙트를 추가하면" 이라고 자리만 비워 뒀다. Java 도 `google_checks.xml`(스타일)뿐이다. 장기 위험 요소를 잡는 도구가 전 언어에 없다 |

두 게이트가 **검사하지 않고 통과한다**는 점에서 성격이 같다. `lint.sh` 는 같은 상황을
`die` 로 막고 있어(도구가 없으면 게이트를 빨간불로 만든다) 저장소 안에서 기준이
엇갈린다. 묶어서 한 번에 보는 편이 낫다.

---

초안(8/5)에 있던 "`GET /places` 에 `personId` 추가" 는 빠졌다. 권호가 MZ2AZ-167 로 다른
방식(`q` 확장)으로 이미 해결했다 — §4 참고.

## 7. 작업 순서

**iOS 를 먼저 만든다.** 준비 비용만 보면 Android 가 가볍지만, iOS 의 준비 작업이
**팀 전체를 멈춰 세우는 종류**라서다 — 정식 Xcode 설치는 세 명이 각자 10~15GB 를 받아야
하고, 그때까지 `just check` 가 도는지 아무도 확신할 수 없다. 이 불확실성은 미룬다고
줄지 않는다. 반대로 Android 는 언제 시작해도 팀의 다른 작업을 건드리지 않는다.

프로토타입이 iOS 시뮬레이터에서 도는 것도 이유다 — §3 의 항목을 화면끼리 바로 대조할 수 있다.

```
[완료] Flutter 프로토타입으로 화면·검색 규칙 확정   ← ~/workspace/mobile
  ↓
[완료] 계획 문서 (이 파일)
  ↓
[완료] 모듈 이름·순서 확정                          ← §6
  ↓
      팀 공지 — 권호·태환도 Xcode 를 깔아야 한다    ← §5-1
  ↓  (설치 확인될 때까지 아래로 못 간다)
      MODULE.bazel Apple 블록 활성화                 ← §5-1
  ↓
      just new-app-ios scenetrip-ios
  ↓
      target_compatible_with 선언 → just check 초록  ← §5-3
  ↓
[완료] CI macOS 잡 추가                            ← §5-4
  ↓
[완료] swift5 클라이언트 생성 타깃 + ADR 0006      ← §5-5
  ↓
      iOS 화면 구현 (§3 을 기준으로)
  ↓
      just check → PR
  ↓
  ─────────── 여기서부터 Android ───────────
      MODULE.bazel Kotlin/Android 블록 활성화        ← §5-1
  ↓
      just new-app-android scenetrip-android
  ↓
      BUILD 파일 손으로 작성 (Gazelle 없음)          ← §5-2
  ↓  기존 ubuntu CI 가 이미 검사한다 — 새 잡 불필요
      Android 화면 구현 (§3 을 같은 기준으로)
  ↓
      두 앱 대조 → just check → PR                   ← §8
```

**§5 를 먼저 끝내야 화면 작업이 시작된다.** 순서를 뒤집으면 소스 파일은 쌓이는데 Bazel 이
그것을 짓지 못하는 상태가 되고, [CLAUDE.md §0](../../../CLAUDE.md) 의 제1법칙("Bazel 이 짓지
못하면 완료가 아니다")에 걸린다.

화면 구현은 §3 의 소제목이 그대로 티켓 단위가 된다 — 화면 구조 / 검색 범위 / 자동완성 /
지도 연동. 각각이 독립적으로 확인 가능하고 크기도 비슷하다.

**서브태스크 하나가 두 플랫폼을 덮는다.** iOS 를 먼저 만들기로 했으므로, 이는 iOS 가 끝나도
서브태스크가 한동안 진행 중으로 남는다는 뜻이다. 그 상태를 감수한다 — iOS 만 된 것을 완료로
찍으면 보드가 "검색 탭 끝남" 으로 보이는데 실제로는 사용자 절반이 못 쓰는 상태다. iOS 가
어디까지 됐는지는 **커밋의 서브태스크 키**가 이미 기록한다(AGENTS.md §7.3). 티켓을 닫는 것과
진척을 남기는 것은 다른 일이다.

Android 를 언제 시작할지가 §6 의 남은 결정 #6 이다. 그 답이 나오면 이 문단을 갱신한다.

### MZ2AZ-148 의 서브태스크와 어떻게 맞물리는가

| 서브태스크 | 이 계획의 어디 |
| --- | --- |
| MZ2AZ-159 테크 스펙 추출·문서화 | 이 문서 §3 · §4 |
| MZ2AZ-160 iOS · Android 프로젝트 초기 구성 | §5-1 · §5-2 · §5-3 · §5-4 |
| MZ2AZ-161 네이버 지도 SDK 연동 | §3-5 (플랫폼별 SDK 는 각자) |
| MZ2AZ-162 첫 진입 지도 화면 | §3-1 |
| MZ2AZ-163 검색 자동완성 UI 연계 | §3-3 · §5-5 |
| MZ2AZ-164 화면 구성 정리 | §3-1 |

## 8. 검증

- `just check` 가 초록인가 — macOS 에서는 iOS 타깃까지, 리눅스에서는 그것을 건너뛰고 Android 는 짓고.
- CI 의 두 검증 잡이 각자 자기 몫을 하는가 — `verify`(리눅스) 로그에서 **iOS 만** 건너뛴 것을 확인한다. Android 까지 건너뛰었다면 §5-3 을 잘못 붙인 것이다. `verify-macos` 로그에서는 그 반대를 본다 — Swift 린트·포맷이 "건너뜁니다" 없이 **실제로 돈** 흔적이 있어야 한다. 양쪽 다 건너뛰었다면 Swift 는 아무 데서도 검사되지 않은 것이다 (§5-4).
- Android 모듈에 소스를 하나 더해 보고 BUILD 파일을 고치지 않으면 빌드에서 빠지는지 확인한다 — §5-2 의 함정을 팀이 실제로 겪어 두는 편이 낫다.
- §3-2 표의 네 줄이 실제 화면에서 다 되는가. 배우 이름 줄은 서버에서 이미 확인됐으므로(`공유`·`김고은`·`도깨비` → 작품 1건 · 장소 3건, §4), 화면에서는 **그 응답이 두 탭에 제대로 갈라져 들어가는지**를 본다. 같은 세 검색어를 그대로 쓰면 서버 결과와 대조할 수 있다.
- §3-6 은 서버를 내려서 확인한다. `500` 에서 재시도 버튼과 `traceId` 가 뜨는가, `400`·`409` 에서는 재시도가 **안** 뜨는가, 어느 코드에서도 서버 `message` 가 화면에 새어 나오지 않는가. 프로토타입에는 이 상태가 없으므로 대조군이 없는 유일한 절이다.
- **두 앱을 나란히 놓고 §3 의 항목을 하나씩 대조한다.** ADR 0002 가 검증 항목으로 건 "두 앱이 어긋나지 않는가"가 이것이다.
- 프로토타입과도 나란히 놓고 화면 동작을 비교한다.

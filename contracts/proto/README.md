# contracts/proto

gRPC service and message definitions. Layout: `<domain>/<version>/<file>.proto`.

- Package names mirror the path: `scenetrip.scene.v1`.
- Never renumber or reuse a field tag; use `reserved`.
- A breaking change means a new version directory.
- Language bindings are generated by Bazel; nothing generated is committed here.

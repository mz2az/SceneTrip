# contracts/proto

gRPC 서비스와 메시지 정의. 배치는 `<도메인>/<버전>/<파일>.proto`.

- 패키지 이름은 경로를 그대로 반영한다: `scenetrip.scene.v1`
- 필드 번호를 재사용하거나 다시 매기지 않는다 — `reserved` 를 쓴다.
- 파괴적 변경은 새 버전 디렉터리를 뜻한다.
- 언어별 바인딩은 Bazel 이 생성한다. 생성물은 여기 커밋하지 않는다.

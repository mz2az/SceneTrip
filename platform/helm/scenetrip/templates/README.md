# 차트 템플릿

`config.yaml`은 API·gateway 설정, `workloads.yaml`은 Deployment·Service,
`ingress.yaml`은 Auto Mode ALB의 IngressClassParams·IngressClass·Ingress,
`network-policy.yaml`은 Pod 통신 경계를 정의한다. 차트 설명은 [상위 문서](../README.md)를 따른다.

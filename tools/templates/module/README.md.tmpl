# {{NAME}}

> 모듈 종류: `{{KIND}}` · 언어: `{{LANG}}` · 경로: `{{PATH}}`

## 목적

이 모듈이 무슨 일을 하고 왜 존재하는지 한 문단. 작성한 뒤 이 줄은 지우세요.

## 인터페이스

| 항목 | 값 |
| --- | --- |
| 프로토콜 | HTTP / gRPC / CLI / 해당 없음 |
| 포트 | 미정 |
| 계약 | `contracts/…` (이 모듈이 구현하거나 사용하는 정의를 나열) |

## 의존성

| 의존 대상 | 이유 |
| --- | --- |
| `libs/…` | |
| `services/…` | 직접 import 가 아니라 계약을 통해 |

## 명령

```bash
just build-module {{PATH}}    # 빌드
just test-module  {{PATH}}    # 테스트
just run //{{PATH}}:bin       # 로컬 실행
```

## 설정

| 환경변수 | 필수 | 기본값 | 용도 |
| --- | --- | --- | --- |

시크릿은 시크릿 매니저에서 온다. 커밋하는 것은 `.env.example` 뿐이다.

## 운영

런북: `docs/ops/` · 대시보드: 미정 · 알림: 미정

#!/usr/bin/env bash
# 두 에이전트 평가 실행 결과를 비교한다.
# 호출: just agent-eval-diff
# shellcheck source=tools/scripts/_lib.sh
source "$(dirname "${BASH_SOURCE[0]}")/_lib.sh"

pending "평가 비교 미구현 — docs/ai/README.md 참조"

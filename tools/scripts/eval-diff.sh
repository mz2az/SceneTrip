#!/usr/bin/env bash
# Compare two agent evaluation runs.
# Invoked by: just agent-eval-diff
# shellcheck source=tools/scripts/_lib.sh
source "$(dirname "${BASH_SOURCE[0]}")/_lib.sh"

pending "eval diffing not implemented — see docs/ai/README.md"

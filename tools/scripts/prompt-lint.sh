#!/usr/bin/env bash
# Validate agent prompt files.
# Invoked by: just agent-lint-prompts
# shellcheck source=tools/scripts/_lib.sh
source "$(dirname "${BASH_SOURCE[0]}")/_lib.sh"

pending "prompt linting not implemented — see docs/ai/README.md"

#!/usr/bin/env bash
# docs 하위 모든 디렉터리에 README 인덱스가 있는지 확인한다.
# 호출: just docs-check-index
# shellcheck source=tools/scripts/_lib.sh
source "$(dirname "${BASH_SOURCE[0]}")/_lib.sh"

pending "문서 인덱스 검사 미구현"
